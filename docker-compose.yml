services:
  api:
    build: .

    container_name: reminder_api

    ports:
      - "3001:3001"

    env_file:
      - .env

    depends_on:
      - redis

  redis:
    image: redis:7

    container_name: reminder_redis

    ports:
      - "6379:6379"